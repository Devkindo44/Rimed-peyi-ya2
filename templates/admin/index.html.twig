{% extends 'base.html.twig' %}

{% block title %}Back office{% endblock %}
{% block h1 %}Back office{% endblock %}

{% block body %}
<div class="row">
    <div class="list-group col-xl-3 col-lg-4 col-md-6">
        <a href="{{ path('app_admin_user_index')}}" class="list-group-item list-group-item-action bg-success text-white" aria-current="true">Gestion des membres
        </a>
        <a href="{{ path('app_user_show', {'id': app.user.id}) }}" class="list-group-item list-group-item-action">Afficher</a>
        <a href="{{path('app_user_new')}}" class="list-group-item list-group-item-action">Ajouter</a>
         <a href="{{path('app_user_edit',{'id': app.user.id} )}}" class="list-group-item list-group-item-action">Ajouter</a>
        <a href=" {{ path('app_user_delete', {'id':app.user.id}) }}" class="list-group-item list-group-item-action">Supprimer</a> 
    </div>
</div>

{% endblock %}
