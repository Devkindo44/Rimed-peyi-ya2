{% extends 'base.html.twig' %}

{% block title %}Log in!{% endblock %}

{% block h1 %}Connexion{% endblock %}

{% block body %}
<form method="post" class="col-md-2 mx-auto p-4 m-4 shadow rounded btn-custom-green">
      {% if error %}

            {% if error.line == 265 or error.line == 67 %}
                <div class="alert alert-danger text-center">L'identifiant ou le mot de passe est incorrect</div>
            {% else %}
                <div class="alert alert-danger text-center">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
            {% endif %}
            
        {% endif %}

    {% if app.user %}
        <div class="mb-3">
            You are logged in as {{ app.user.userIdentifier }}, <a href="{{ logout_path() }}">Logout</a>
        </div>
    {% endif %}

    <h1 class="h3 mb-3 font-weight-normal">Please sign in</h1>
    <label for="inputEmail">Email</label>
    <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" autocomplete="email" required autofocus>
    <label for="inputPassword">Password</label>
    <input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required>
    <input type="hidden" name="_csrf_token" data-controller="csrf-protection" value="{{ csrf_token('authenticate') }}">

    <div class="checkbox mb-3">
        <label>
            <input type="checkbox" name="_remember_me"> Se souvenir de moi
        </label>
    </div>

    <button class="btn btn-lg btn-custom-brown" type="submit">
        Sign in
    </button>
</form>
{% endblock %}
