{% extends 'base.html.twig' %}

{% block title %}Recap{% endblock %}

{% block body %}
<div class="container mt-4 px-2">
    <h2>Récapitulatif de votre commande</h2>

    <table class="table  vertical-align-middle mb-0 table-bordered table-hover text-center  text-white mt-4 bg-success ">
        <thead class="border border-success">
            <tr>
                <th class="border border-success">Image</th>
         
              
                <th>Produit</th>
           
                <th class="text-center">Quantité</th>
                <th class="text-end">Prix Unitaire</th>
                <th class="text-end">Total</th>
            </tr>
        </thead>
        <tbody>
        {% for ligne in commande.ligneDeCommandes %}
            <tr>
                {# 1. COLONNE IMAGE #}
                <td>
                    {% if ligne.product.illustration %}
                        <img src="{{ asset('images/' ~ ligne.product.illustration) }}" 
                             alt="{{ ligne.product.title }}" 
                             style="width: 80px; height: 80px; object-fit: cover; border-radius: 5px;">
                    {% else %}
                        {# Image par défaut si le produit n'en a pas #}
                        <img src="{{ asset('images/default-product.png') }}" 
                             alt="Pas d'image" 
                             style="width: 50px; height: 50px; object-fit: cover; border-radius: 5px;">
                    {% endif %}
                </td>

                {# 2. RESTE DES INFORMATIONS #}
                <td>{{ ligne.product.title }}</td> 
                <td class="text-center">{{ ligne.quantity }}</td> 
                <td class="text-end">{{ ligne.prixUnitaire }} €</td>
                <td class="text-end">{{ (ligne.prixUnitaire * ligne.quantity) }} €</td>
            </tr>
        {% endfor %}
        {# --- LIGNE DU MONTANT TOTAL --- #}
        <tr class="table-light fw-bold" style="border-top: 2px solid #dee2e6;">
            <td colspan="3" class="text-end">Montant Total :</td>
            <td colspan="2" class="text-end text-custom-brown" style="font-size: 1.1rem;">
                {# On appelle le getter de ton entité Commande (ajuste le nom si nécessaire, ex: montantTotal) #}
                {{ commande.montantTotal }} €
            </td>
        </tr>
        </tbody>
    </table>
</div>
{% endblock %}