<header>
    <nav class="navbar navbar-expand-lg bg-custom-green py-3">
        <div class="container">
            <a class="navbar-brand text-white" href="{{ path('app_home') }}">
                <img src="{{ asset('/images/logo_Projet.png') }}" alt="Logo" width="80">
            </a>

            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_boutique') }}">Boutique</a>
                    </li>

                    {# MENU DÉROULANT UNIQUE POUR LES CATÉGORIES 
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle text-white" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Nos catégories
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <li>
                                <a class="dropdown-item" href="{{ path('app_categories/infusions') }}">Infusions</a>
                            </li>
                            <li>
                                <a class="dropdown-item" href="{{ path('app_categories/sirops') }}">Sirops</a>
                            </li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <a class="dropdown-item" href="{{ path('app_categories') }}">Voir tout</a>
                            </li>
                        </ul>
                    </li>
                    #}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Catégories
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="{{ path('app_categories/infusions') }}">Infusions</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_categories/sirops') }}">Sirops</a></li>
                            
                            <li><a class="dropdown-item" href="{{ path('app_categories') }}">Toutes les catégories</a></li>
                        </ul>
                    </li>
                  
                <!-- UL de scond niveau -->
                        {#
                     <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_categories') }}">Categories</a>
                        <ul>
                             <li class="nav-item">
                                <a href="{{ path('app_categories/infusions') }}">Infusions</a>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_categories/sirops') }}">Sirops</a>
                        </ul>
                    </li>
                    #}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_contact') }}">Contactez-Nous</a>
                    </li>
                </ul>
                

                <div class="d-flex gap-3">
                    <a href="{{path('app_login')}}" class="nav-link text-white">Connexion</a>
                    <a href="{{path('app_register')}}" class="nav-link text-white border border-white rounded px-2">Inscription</a>
                </div>
            </div>
        </div>
    </nav>
</header>